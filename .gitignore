# 构建产物：可由源码重建，不入库
/dist/
/bin/*/novaaimcpd

# 手工回滚快照。git 接管回滚后不再需要，保留在磁盘但不入库
/.backup/

# WorkBuddy 的项目级会话记忆（MEMORY.md 与按日工作日志）。
# 它是**本地工具状态**，不是本仓库的源码或文档：内容是"这次改了什么、
# 踩过什么坑"的过程记录，面向的是下一轮开发会话，不是仓库的使用者。
# 保留在磁盘上（不要删），但不入库。
/.workbuddy/

# 编辑器 / 系统杂项
*.swp
*.tmp
Thumbs.db
desktop.ini
