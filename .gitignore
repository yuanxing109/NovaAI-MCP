# 从 v0.07 起本仓库只负责**模块的打包与发布**，不再编译：
#
#   /bin/<abi>/novaaimcpd  三个 ABI 的编译产物 —— 由本地编译后**入库**
#                          （此前是忽略的；现在 CI 不编译，包里的二进制就来自
#                          仓库里这一份，所以必须提交，升版本时要一起 commit）
#   /dist/                 本地打包出的模块 zip 与校验和，不入库（Release 资产）
#
# 源码 /src/ 也不入库：它只在本地开发工作区里。
#   · 依赖源码的东西（build.sh all / build.ps1 all 的编译段、go test、
#     6 个审计与探针脚本）都只能在有源码的机器上跑，见 docs/CI.md
#   · build.sh / build.ps1 的 `package` 模式不碰源码，是本仓库的打包入口
/src/

# 本地打包产物（模块 zip 与 .sha256）——发布时作为 Release 资产上传
/dist/

# 手工回滚快照。git 接管回滚后不再需要，保留在磁盘但不入库
/.backup/

# WorkBuddy 的项目级会话记忆（MEMORY.md 与按日工作日志）。
# 它是**本地工具状态**，不是本仓库的源码或文档：内容是"这次改了什么、
# 踩过什么坑"的过程记录，面向的是下一轮开发会话，不是仓库的使用者。
# 保留在磁盘上（不要删），但不入库。
/.workbuddy/

# 编辑器 / 系统杂项
*.swp
*.tmp
Thumbs.db
desktop.ini
